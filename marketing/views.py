from django.shortcuts import render
from django.views.generic.edit import CreateView
from .models import Subscriber
# Create your views here.
# I think it's now time to beautify the thing and finish it op